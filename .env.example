# URL de tu backend FastAPI (api.py). En desarrollo local suele ser:
VITE_API_URL=http://localhost:8000

# El mismo GOOGLE_CLIENT_ID que ya usa api.py (variable pública, va en el bundle)
VITE_GOOGLE_CLIENT_ID=
